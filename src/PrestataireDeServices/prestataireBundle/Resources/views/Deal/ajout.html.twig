{# empty Twig template #}
{% extends 'UserBundle::layout.html.twig' %}

{% block container %}
    <div class="container">

        <div class="row">
         <div class="box">
                <div class="col-lg-12">
        <h1>Inscription</h1>
        <link href="{{asset('css/inscri.css')}}" rel="stylesheet">
        {{ form(form) }}

        <ul class="record_actions">
            <li>
               <form action="{{ path('deal_create')}}" {{ form_enctype(form) }} method="POST"> 
                   {{ form_widget(form)}}
                   <div>
                       <input type="submit" value="Ajouter" />
                   </div>
              </form>
     
            </li>
        </ul>
    </div>
         </div>
        </div>
    </div>
{% endblock %}

